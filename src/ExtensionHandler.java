public class ExtensionHandler {
     /*
    switch (getExtension(file)) {
        case "jpg":
        case "png":
        case "ico":
            return true;
        default:
            return false;
    }

    // colocar aqui depois metodos para meter as extensoes como deve de ser
    // (substitui o metodo switch que tenho no server)


      */
}
